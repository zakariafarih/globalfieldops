spring:
  datasource:
    url: jdbc:tc:postgresql:16-alpine:///workorder_db
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  flyway:
    enabled: true
  jpa:
    hibernate:
      ddl-auto: validate

services:
  technician-service:
    url: http://localhost:0
